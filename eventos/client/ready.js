
// Iniciando o evento ready
module.exports = async (client) => {  // Aqui sempre utilize (client, <outros parâmetros>)

    // Logando no console que o bot iniciou
    console.log('Bot iniciado com sucesso!')

}